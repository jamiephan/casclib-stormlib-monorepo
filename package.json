{
  "name": "@jamiephan/monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Monorepo for CascLib and StormLib native bindings",
  "repository": {
    "type": "git",
    "url": "https://github.com/jamiephan/casclib-stormlib-monorepo.git"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r --stream build",
    "test": "pnpm -r --stream test",
    "clean": "pnpm -r --stream clean",
    "rebuild": "pnpm -r --stream rebuild",
    "test:coverage": "pnpm -r --stream test:coverage"
  },
  "devDependencies": {
    "@types/jest": "^29.5.11",
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.0"
}
